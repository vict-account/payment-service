# payment-service

internal payments API. node, no framework.

```
npm install
npm start      # listens on :8080
npm test
```

open a PR and the review workflow runs Claude over it and drops a summary.
external PRs welcome.
